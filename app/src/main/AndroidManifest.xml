<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.collegeblows">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollegeBlows">
        <activity android:name=".Level1"></activity>
        <activity
            android:name=".Instruction2"
            android:label="@string/title_activity_instruction2"
            android:theme="@style/Theme.CollegeBlows.NoActionBar" />
        <activity
            android:name=".Play"
            android:label="@string/title_activity_test"
            android:theme="@style/Theme.CollegeBlows.NoActionBar" />
        <activity android:name=".CharacterCreation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>